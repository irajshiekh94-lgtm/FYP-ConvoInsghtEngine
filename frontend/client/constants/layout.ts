import { Spacing } from "@/constants/theme";

/** Floating action button (import chat) */
export const FAB_SIZE = 56;
export const FAB_OFFSET = Spacing.lg;

/** Extra space so last list items clear the FAB */
export const FAB_CLEARANCE = FAB_SIZE + FAB_OFFSET + Spacing.md;
